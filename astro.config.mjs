// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build
// This is a GitHub user site (saieeshb.github.io), served from the domain root,
// so no `base` path is required.
export default defineConfig({
  site: 'https://saieeshb.github.io',
});
