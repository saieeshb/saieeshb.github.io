// @ts-check
import { defineConfig } from 'astro/config';

// https://astro.build
// This is a GitHub user site (saieesh.dev), served from the domain root,
// so no `base` path is required.
export default defineConfig({
  site: 'https://saieesh.dev',
});
